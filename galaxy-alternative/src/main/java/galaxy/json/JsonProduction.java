package galaxy.json;

import com.fasterxml.jackson.databind.JsonNode;
import galaxy.planet.Production;

public class JsonProduction implements Production {
	public JsonProduction(JsonNode src) {
	}
}
